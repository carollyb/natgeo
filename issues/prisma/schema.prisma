generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model Issue {
  id String @id @default(uuid()) @map("_id")
  number Int
  date DateTime
  cover String
  file String
  language String
  topics String

  @@map("issue")
}